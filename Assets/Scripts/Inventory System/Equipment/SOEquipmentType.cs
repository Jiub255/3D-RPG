using UnityEngine;

// Treat these like equipment type enums, just use the SO's name as the enum. 
[CreateAssetMenu(menuName = "Equipment/Equipment Type", fileName = "New Equipment Type")]
public class SOEquipmentType : ScriptableObject{}